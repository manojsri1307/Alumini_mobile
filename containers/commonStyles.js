import { Colors } from "react-native/Libraries/NewAppScreen";

const cm = {
    textInput: {
        borderWidth: 1,
        borderColor: '#DCDCDC',
        borderRadius: 8,
        width: '80%',
        height: 50,
        fontSize: 14,
        paddingLeft: 15,
        paddingRight: 6,
        color: "#000"
    }
};

export default cm;
